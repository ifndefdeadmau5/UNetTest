using UnityEngine;
using System.Collections;
using System.Collections.Generic;


public class UserManager : MonoBehaviour {
    // 나중에 지울 필요가 있는 코드들...
    /*
    IDictionary<string, GameObject> dic = new Dictionary<string, GameObject> { };
    GameObject GetPrefab;
    public UIPanel Panel;
    // Use this for initialization
    public void AddObject(string address, GameObject prefab )
    {
        Debug.Log("address: " + address + "GO ID:" + prefab.GetInstanceID( ) );
        dic.Add(address, prefab);
        Debug.Log("prefab added");
    }
    */
}
